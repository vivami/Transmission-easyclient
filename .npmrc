; React 18 is newer than react-tiny-popover@4's declared peer range (^16).
; The library works with React 18 at runtime; this bridges `npm install` until
; the UI libraries are bumped to their React-18-aware majors (modernization
; Phase 5). All other dependencies resolve cleanly against React 18.
legacy-peer-deps=true
